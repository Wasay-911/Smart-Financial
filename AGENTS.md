# AGENTS.md

## Cursor Cloud specific instructions

This repository (`Smart-Financial`) is currently an empty scaffold with only a `README.md`. There is no application code, no dependency manifest, no build system, no tests, and no services to run.

### Current state

- **No package manager or dependencies** — no `package.json`, `requirements.txt`, `pyproject.toml`, or similar.
- **No application code** — no source files of any kind.
- **No CI/CD** — no GitHub Actions, GitLab CI, or other pipeline configuration.
- **No Docker/container setup** — no `Dockerfile` or `docker-compose.yml`.

### For future agents

Once application code is added, update this file with:
- How to install dependencies and run the dev server.
- How to run lint, tests, and builds.
- Any non-obvious environment requirements or gotchas.
